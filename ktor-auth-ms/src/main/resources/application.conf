ktor {
    deployment {
        test = false
        mongodbpassword = Nnmah8cfhYVDiuIu
        sslPort = 443
    }

    application {
        modules = [ com.example.ApplicationKt.module ]
    }

    security {
            ssl {
                keyStore = cert/keystore.jks
                keyAlias = sampleAlias
                keyStorePassword = foobar
                privateKeyPassword = foobar
            }
        }
}

jwt {
    secret = "secret"
    issuer = "http://0.0.0.0:443/"
    audience = "http://0.0.0.0:443/hello"
    realm = "Access to 'hello'"
}
